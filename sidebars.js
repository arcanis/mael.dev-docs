// @ts-check

const baseConfig = require(`./mael.config`);

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  sidebar: baseConfig.sidebar,
};

module.exports = sidebars;
